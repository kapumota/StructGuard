package generic.cpp;

structure Vector {
  invariant n >= 0;
  invariant n <= capacity();
  method get requires 0 <= i && i < n;
  method set requires 0 <= i && i < n;
  method add { requires 0 <= i && i <= n; ensures n == old(n) + 1; }
  method remove { requires 0 <= i && i < n; ensures n == old(n) - 1; }
}
