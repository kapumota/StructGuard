### Pruebas generadas

Esta carpeta se usa como destino por defecto para candidatos generados por:

```bash
structguard testgen ... --test-dir generated_tests
```

Los archivos producidos son candidatos de regresión. Deben revisarse, compilarse y ajustarse antes de tratarlos como pruebas definitivas.
